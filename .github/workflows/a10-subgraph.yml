name: A10 Subgraph

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/a10-subgraph.yml'
      - 'deployments/schema.json'
      - 'packages/contracts/abi/**'
      - 'packages/contracts/artifacts.json'
      - 'packages/subgraph/**'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/a10-subgraph.yml'
      - 'deployments/schema.json'
      - 'packages/contracts/abi/**'
      - 'packages/contracts/artifacts.json'
      - 'packages/subgraph/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: a10-subgraph-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Run fixed A10 behavior contract
        run: npm test --prefix packages/subgraph
