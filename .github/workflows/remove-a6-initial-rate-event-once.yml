name: Remove redundant A6 initial rate event once

on:
  push:
    branches:
      - gpt56/issue-8-pdp-capacity-pricing
    paths:
      - .github/remove-a6-initial-rate-event-once

permissions:
  contents: write

jobs:
  reduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: gpt56/issue-8-pdp-capacity-pricing
          fetch-depth: 0
          submodules: recursive

      - run: python3 .github/remove-a6-initial-rate-event.py

      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.3.5
          cache: true

      - run: forge fmt

      - name: Commit reduction and remove one-shot files
        shell: bash
        run: |
          set -euo pipefail
          rm -f \
            .github/remove-a6-initial-rate-event-once \
            .github/remove-a6-initial-rate-event.py \
            .github/workflows/remove-a6-initial-rate-event-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "refactor: avoid redundant initial rate event"
          git push origin HEAD:gpt56/issue-8-pdp-capacity-pricing
